name: Postgres
description: Postgres docker container
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ env.POSTGRES_DB }}
    - ${{ env.POSTGRES_USER }}
    - ${{ env.POSTGRES_PASSWORD }}
    - ${{ env.DB_PORT }}